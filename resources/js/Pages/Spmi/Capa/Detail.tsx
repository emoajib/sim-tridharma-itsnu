/**
 * CAPA Detail Page.
 *
 * NOTE: The backend controller renders `Spmi/Capa/Show` for the show route.
 * This file is provided as an alias. To use this file, update the CapaController
 * to render `Spmi/Capa/Detail` instead of `Spmi/Capa/Show`.
 *
 * For now, the actual implementation is in Show.tsx which is already configured
 * to work with the backend's `route('spmi.capa.show', ...)`.
 */
export { default } from './Show';
